export const HOURS = Array.from({ length: 25 }, (_, i) => i);
export const MIN_CONTENT_WIDTH = 600;
export const HOUR_HEIGHT = 120;
export const TIME_COLUMN_WIDTH = 80;
export const MIN_SESSION_HEIGHT_PX = 80;
